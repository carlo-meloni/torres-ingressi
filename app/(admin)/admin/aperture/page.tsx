import { PagePlaceholder } from "@/components/admin/PagePlaceholder";

export default function AperturePage() {
  return (
    <PagePlaceholder
      title="Aperture"
      description="Apri e chiudi le finestre di apertura e genera i turni."
    />
  );
}
